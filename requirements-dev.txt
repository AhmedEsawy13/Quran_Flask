# Development / test dependencies (not needed at runtime).
# Install with: python3 -m pip install -r requirements-dev.txt
-r requirements.txt
pytest>=8.0
rapidfuzz>=3.0
# Official offline Tafsir MCP database for Quran meaning comparisons.
tafsir-mcp>=1.0.0
-r requirements-font-audit.txt

# pipeline/build_classical_llm.py --api only (classical waqf AI re-extraction).
# Pick whichever provider you're using — only the one you call needs installing.
anthropic>=0.40.0     # --provider anthropic (default)
google-genai>=0.3.0   # --provider gemini
python-dotenv>=1.0.0  # auto-loads ANTHROPIC_API_KEY / GEMINI_API_KEY from a local .env
